# 作業ルール

- ユーザーへの応答とプロジェクト文書は日本語で記述する。
- ソース、文書、スクリプトはUTF-8で保存する。
- CubeMX生成ファイルでは`USER CODE BEGIN`／`USER CODE END`を優先し、再生成後も変更が残る構成にする。
- 生成コードを変更した場合は、対応する`mother_4steer.ioc`との不一致と再生成時の影響を確認する。
- ルート`CMakeLists.txt`はユーザー管理、`cmake/stm32cubemx/CMakeLists.txt`はCubeMX管理として扱う。
- 新しいユーザーコードのコメントは日本語で、処理の目的、前提、制約を説明する。CubeMX生成コメントは維持し、コードをそのまま言い換えるコメントは追加しない。
- 実装済み、設定済み、実機検証済みを区別する。ピン名や既存設定から制御仕様、通信形式、タイミングを推測して確定しない。
- 変更に応じてREADMEと`doc/`を更新し、記載したコマンドとスクリプトの引数を一致させる。
- 検証は変更範囲に応じて行う。通常は対象構成のビルドを実行し、CMakeやツールチェーン変更時はDebug／Releaseとクリーン構成を確認する。
- 実機書き込みや外部機器を動かす検証は、対象と条件が明確な場合に行い、結果と未確認範囲を記録する。
